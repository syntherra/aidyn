# Cross-Browser Verification

- Chrome latest: layout, fonts, shadows, flex behavior.
- Safari latest: font rendering, button radius, focus styles.
- Firefox latest: flex gaps, box-shadow, input placeholders.
- Edge latest: SVG assets, keyboard navigation.
- Accessibility: screen readers announce labels, errors via `aria-describedby` and `role=alert`.
- Performance: assets SVG, no layout shift, inputs render fast.

