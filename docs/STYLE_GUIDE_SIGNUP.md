# Sign Up Page Style Guide

- Layout: two-column container, left promo and right card, max-width `1512px`, padding `50px` outer, card padding `50px 90px`, radius `16px`, shadow `0 0 50px 0 #0000001a`.
- Colors: text `#000000`, secondary `#00000066`, bg `#ffffff`, input bg `#f4f4f4`, lines `#bbbbbb`, dots `#b4b4b4`, accent `#ff772d`.
- Typography: Inter family, title `32px 700`, subtitle/body `14px`, inputs `16px`, uppercase tagline `24px`.
- Components: inputs `60px` height, rounded `16px`, button primary orange `#ff772d` white text `14px 700`, checkbox `19px` square with `8px` radius.
- Spacing: input padding `10px 40px`, row gaps `10px`, tagline line-height `116px`.
- Responsive: stack columns under `1024px`, card padding `24px` on mobile, maintain readable line lengths.

